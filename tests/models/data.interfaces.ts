// Inerfaces will be migrated to it's own data type file once we will have more data types in the app
export interface Product {
  itemName: string;
  price: number;
}
export interface ConsumerDetails {
  firstName: string;
  lastName: string;
  postalCode: string;
}
